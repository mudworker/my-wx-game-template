import config from '../config'

export default class Preload {
    ctx
    constructor(ctx, key = 'preload') {
        this.ctx = ctx
        this.key = key
    }

    start() {
        
    }

    destory() {

    }
}